Python parser that allows track prices and buy in real time via exchange web interface.

Don't forget to install following dependencies:
 
sudo pip3 install requests
sudo pip3 install selenium
sudo pip3 install python-telegram-bot
